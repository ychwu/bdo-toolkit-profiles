# Immutable NA/EU profiles

Each `REVISION.json` file is an immutable version-1 profile envelope. Once
published, never change or delete its bytes. Corrections use a new revision so
historical PCAP replay remains reproducible.

